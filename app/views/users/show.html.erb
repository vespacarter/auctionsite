<p><h2><%= @user.name %></h2>
<h5><%= @user.email %></h5>
<%= link_to("Add a Product", new_product_path) %> - <%= link_to("Delete User", user_path(@user.id), method: :delete) %></p>

<p>
List of user products (<%= @products.count %>):
<ul>
	<% @products.each do |product| %>
		<li><h4><%= product.id %>: <b><%= link_to( product.title , product_path(product.id)) %></h4>
		<ul>
		<li><h5><i><%= product.description %></i></h5></li>
		<li><h5>Ends <%= product.deadline %></h5></li>
		</ul></li>
	<% end %>
	<br>
</ul>
</p>
