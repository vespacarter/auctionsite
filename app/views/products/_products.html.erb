<div class="col-md-8 col-md-offset-2">
<% counter = 0 %>
<div class="row">
	<% @products.each do |product| %>
		<div class="col-md-4 square-item">
		<div class="item-title"><h4><b><%= link_to( product.title, product_path(product.id)) %></b></h4></div>
		<ul class="list-unstyled item-content">
		<li><h5><i><%= product.description %></i></h5></li>
		<li><h6>Ends <%= product.deadline %></h6></li>
		</ul></li>
		</div>
		<% counter += 1 %>
		<% if (counter % 3) == 0 %>
			</div>
			<br>
			<div class="row">
		<% end %>
	<% end %>
	<br>
</div>
</div>

